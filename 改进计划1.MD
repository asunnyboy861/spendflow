# SpendFlow 应用改进计划（修复版）

**文档版本**：v1.1  
**创建日期**：2026-04-16  
**修复日期**：2026-04-16  
**目标版本**：v1.1.0  
**预计完成时间**：4-6周

---

## 📋 修复说明

本文档已修复以下问题：
1. ✅ 文件结构与现有代码保持一致
2. ✅ CoreDataStack 支持测试模式
3. ✅ CloudSyncService 通过 Repository 操作数据
4. ✅ 添加缺失的 import 语句
5. ✅ 删除重复的 MaterialCard，使用现有 cardStyle()
6. ✅ 确认 Date+Extensions.swift 已有 timeFormat 方法
7. ✅ 统一代码风格

---

## 一、改进目标概述

### 1.1 核心目标

基于对 SpendFlow 应用的深度分析，本次改进旨在：

1. **提升市场竞争力**：补齐与主流预算 APP 的功能差距
2. **优化用户体验**：减少使用摩擦，提升数据安全性
3. **增强智能化**：添加预测和建议功能，帮助用户更好地管理预算
4. **保持简洁性**：在增加功能的同时，保持应用的核心价值主张

### 1.2 改进优先级

| 优先级 | 功能模块 | 预计工作量 | 用户价值 | 技术难度 |
|--------|---------|-----------|---------|---------|
| P0 | iCloud 同步（可选） | 5天 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| P0 | CSV 导出 | 2天 | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| P1 | 周视图 | 3天 | ⭐⭐⭐⭐ | ⭐⭐ |
| P1 | 智能分类建议 | 4天 | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| P1 | 消费预测 | 3天 | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| P2 | 发薪日提醒 | 2天 | ⭐⭐⭐ | ⭐⭐ |

---

## 二、改进原则

### 2.1 功能模块原则

**原则描述**：一个功能一个模块，遵循单一职责、高内聚低耦合、命名清晰、文件结构语义化。

**实施标准**：
- ✅ 每个功能模块独立文件夹
- ✅ 模块内包含：Views、Components、Services
- ✅ **ViewModels 统一放在 `Core/Presentation/ViewModels/`**（与现有代码保持一致）
- ✅ 模块间通过协议和依赖注入通信
- ✅ 文件命名清晰，见名知意

**文件结构示例**（修复后）：
```
Features/
├── Sync/                    # iCloud 同步模块
│   ├── Views/
│   │   ├── SyncSettingsView.swift
│   │   └── Components/
│   │       ├── SyncStatusIndicator.swift
│   │       └── SyncToggle.swift
│   └── Services/
│       ├── SyncService.swift
│       ├── LocalSyncService.swift
│       └── CloudSyncService.swift
├── Export/                  # CSV 导出模块
│   ├── Views/
│   │   ├── ExportView.swift
│   │   └── Components/
│   │       └── ExportOptionsView.swift
│   └── Services/
│       ├── CSVExportService.swift
│       └── CSVBuilder.swift
└── Insights/                # 智能洞察模块
    ├── Views/
    │   ├── WeeklyBudgetView.swift
    │   └── Components/
    │       └── PredictionCard.swift
    └── Services/
        ├── CategorySuggester.swift
        └── BudgetPredictor.swift

Core/
└── Presentation/
    └── ViewModels/          # 所有 ViewModel 统一放在这里
        ├── SyncViewModel.swift
        ├── ExportViewModel.swift
        ├── WeeklyBudgetViewModel.swift
        └── PredictionViewModel.swift
```

### 2.2 最小代码原则

**原则描述**：优先复用现有模块，抽象需符合"三次法则"（遇到三次再抽），避免过度设计。

**实施标准**：
- ✅ 新功能优先查找现有组件
- ✅ 相同逻辑出现3次才抽象
- ✅ 使用 extension 扩展现有类型
- ✅ 避免创建重复的工具类

**代码复用示例**：
```swift
// ❌ 错误：创建新的导出按钮
struct ExportButton: View {
    var body: some View {
        Button("Export") { /* ... */ }
    }
}

// ✅ 正确：复用现有的 HapticButton
HapticButton("Export CSV", style: .secondary) {
    exportService.export()
}
```

### 2.3 重构清理原则

**原则描述**：替换时标记废弃 → 验证无影响 → 删除，仓库提交信息注明清理范围。

**实施流程**：
1. **标记废弃**：使用 `@available(*, deprecated)` 标记
2. **验证测试**：运行所有测试用例，确保无影响
3. **删除代码**：确认无引用后删除
4. **提交说明**：`refactor: 移除废弃的 XXX 模块，已由 YYY 替代`

### 2.4 风格一致性原则

**原则描述**：优化内容需与当前软件风格一致（格调、功能、UI 等）。

**实施标准**：
- ✅ 使用现有的 `DesignTokens` 设计系统
- ✅ 遵循现有的颜色编码（红=支出，绿=收入，橙=警告）
- ✅ **使用现有的 `cardStyle()` ViewModifier**（已存在于 View+Extensions.swift）
- ✅ 使用现有的组件（HapticButton、EmptyStateView 等）
- ✅ 保持相同的动画风格和交互模式

**UI 风格示例**：
```swift
// ✅ 正确：使用现有设计系统
struct NewFeatureCard: View {
    var body: some View {
        VStack(spacing: DesignTokens.Spacing.m) {
            Text("New Feature")
                .font(.headline)
            // ...
        }
        .cardStyle()  // 使用现有的卡片样式
    }
}

// ❌ 错误：创建新的样式
struct NewFeatureCard: View {
    var body: some View {
        VStack(spacing: 20) {  // 硬编码间距
            Text("New Feature")
                .font(.custom("Helvetica", size: 18))  // 自定义字体
            // ...
        }
        .background(Color.blue)  // 自定义颜色
        .cornerRadius(15)  // 硬编码圆角
    }
}
```

### 2.5 继承兼容原则

**原则描述**：优化内容需考虑继承关系，优先继承现有代码，避免硬编码。

**实施标准**：
- ✅ 新 ViewModel 遵循现有 ViewModel 的模式
- ✅ 新 Repository 实现现有 Repository 协议
- ✅ 新 View 复用现有的 ViewModifier
- ✅ 使用依赖注入而非硬编码依赖
- ✅ **通过 Repository 操作数据，不直接操作 CoreData**

**继承示例**：
```swift
// ✅ 正确：遵循现有模式，使用依赖注入
class SyncViewModel: ObservableObject {
    @Published var syncStatus: SyncStatus = .notConfigured
    
    private let syncService: SyncService
    private var cancellables = Set<AnyCancellable>()
    
    init(syncService: SyncService) {
        self.syncService = syncService
        setupBindings()
    }
}

// ❌ 错误：硬编码依赖
class SyncViewModel: ObservableObject {
    let syncService = CloudSyncService()  // 硬编码
}
```

---

## 三、功能模块详细方案

---

## 3.1 iCloud 同步功能（P0）

### 3.1.1 需求描述

**用户故事**：
> 作为用户，我希望能够选择性地开启 iCloud 同步，以便在多个设备间同步我的预算数据。同时，我希望在模拟器或没有 iCloud 的环境下也能正常使用应用。

**功能要点**：
1. 默认使用本地存储（CoreData）
2. 用户在设置中手动开启 iCloud 同步
3. 支持 iCloud 和本地存储的无缝切换
4. 提供同步状态指示器
5. 支持冲突解决（最后写入优先）

**非功能需求**：
- 性能：同步操作不阻塞 UI
- 可靠性：网络失败时自动重试
- 安全性：数据加密传输
- 兼容性：iOS 15+，支持模拟器

### 3.1.2 架构设计

**设计模式**：策略模式（Strategy Pattern）

```
┌─────────────────────────────────────┐
│      SyncService (Protocol)         │
│  - syncTransactions()               │
│  - syncBudgets()                    │
│  - syncAccounts()                   │
│  - getSyncStatus()                  │
└─────────────────────────────────────┘
              ↑
              │ implements
    ┌─────────┴──────────┐
    │                    │
┌───┴────────┐    ┌─────┴─────────┐
│ LocalSync  │    │ CloudSync     │
│ Service    │    │ Service       │
│ (CoreData) │    │ (CloudKit)    │
└────────────┘    └───────────────┘
```

**数据流**：
```
User Action → SyncViewModel → SyncService
                                   ↓
                            ┌──────┴──────┐
                            │             │
                        LocalSync    CloudSync
                            │             │
                        Repository    Repository
                            │             │
                        CoreData     CloudKit
                            │             │
                            └──────┬──────┘
                                   ↓
                            SyncStatus (Published)
```

### 3.1.3 文件结构（修复后）

```
Features/
└── Sync/
    ├── Views/
    │   ├── SyncSettingsView.swift          # 同步设置界面
    │   └── Components/
    │       ├── SyncStatusIndicator.swift   # 同步状态指示器
    │       └── SyncToggle.swift            # 同步开关
    └── Services/
        ├── SyncService.swift               # 同步服务协议
        ├── LocalSyncService.swift          # 本地同步实现
        └── CloudSyncService.swift          # iCloud 同步实现

Core/
├── Domain/
│   └── Models/                             # 新增：同步相关模型
│       ├── SyncStatus.swift                # 同步状态枚举
│       └── SyncError.swift                 # 同步错误类型
└── Presentation/
    └── ViewModels/
        └── SyncViewModel.swift             # 同步视图模型
```

### 3.1.4 代码实现

#### 3.1.4.1 同步状态模型（修复：添加 import）

**文件**：`Core/Domain/Models/SyncStatus.swift`

```swift
import Foundation
import SwiftUI

enum SyncStatus: Equatable {
    case notConfigured
    case disabled
    case syncing
    case synced(Date)
    case failed(SyncError)
    case conflict
    
    var displayText: String {
        switch self {
        case .notConfigured:
            return "Not Configured"
        case .disabled:
            return "Disabled"
        case .syncing:
            return "Syncing..."
        case .synced(let date):
            let formatter = RelativeDateTimeFormatter()
            return "Synced \(formatter.localizedString(for: date, relativeTo: Date()))"
        case .failed(let error):
            return "Failed: \(error.localizedDescription)"
        case .conflict:
            return "Conflict Detected"
        }
    }
    
    var icon: String {
        switch self {
        case .notConfigured, .disabled:
            return "icloud.slash"
        case .syncing:
            return "arrow.triangle.2.circlepath"
        case .synced:
            return "checkmark.icloud"
        case .failed, .conflict:
            return "exclamationmark.icloud"
        }
    }
    
    var color: Color {
        switch self {
        case .notConfigured, .disabled:
            return .secondary
        case .syncing:
            return .accentBlue
        case .synced:
            return .incomeGreen
        case .failed, .conflict:
            return .expenseRed
        }
    }
}
```

**测试用例**：
```swift
import XCTest
@testable import SpendFlow

class SyncStatusTests: XCTestCase {
    
    func testSyncStatusDisplayText() {
        let status = SyncStatus.synced(Date())
        XCTAssertTrue(status.displayText.contains("Synced"))
    }
    
    func testSyncStatusEquatable() {
        let status1 = SyncStatus.syncing
        let status2 = SyncStatus.syncing
        XCTAssertEqual(status1, status2)
    }
    
    func testSyncStatusIcon() {
        let status = SyncStatus.disabled
        XCTAssertEqual(status.icon, "icloud.slash")
    }
}
```

#### 3.1.4.2 同步错误类型

**文件**：`Core/Domain/Models/SyncError.swift`

```swift
import Foundation

enum SyncError: Error, Equatable {
    case notSignedIn
    case networkUnavailable
    case quotaExceeded
    case conflictDetected
    case unknown(String)
    
    var localizedDescription: String {
        switch self {
        case .notSignedIn:
            return "Please sign in to iCloud to enable sync"
        case .networkUnavailable:
            return "Network unavailable. Please check your connection"
        case .quotaExceeded:
            return "iCloud storage quota exceeded"
        case .conflictDetected:
            return "Sync conflict detected. Please resolve manually"
        case .unknown(let message):
            return "Sync failed: \(message)"
        }
    }
    
    static func == (lhs: SyncError, rhs: SyncError) -> Bool {
        switch (lhs, rhs) {
        case (.notSignedIn, .notSignedIn),
             (.networkUnavailable, .networkUnavailable),
             (.quotaExceeded, .quotaExceeded),
             (.conflictDetected, .conflictDetected):
            return true
        case (.unknown(let lhsMsg), .unknown(let rhsMsg)):
            return lhsMsg == rhsMsg
        default:
            return false
        }
    }
}
```

#### 3.1.4.3 同步服务协议

**文件**：`Features/Sync/Services/SyncService.swift`

```swift
import Foundation
import Combine

protocol SyncService {
    var syncStatusPublisher: AnyPublisher<SyncStatus, Never> { get }
    var isEnabled: Bool { get set }
    
    func enableSync() async throws
    func disableSync() async throws
    func syncNow() async throws
    func resolveConflict(with resolution: ConflictResolution) async throws
}

enum ConflictResolution {
    case keepLocal
    case keepRemote
    case merge
}
```

#### 3.1.4.4 本地同步服务

**文件**：`Features/Sync/Services/LocalSyncService.swift`

```swift
import Foundation
import Combine

class LocalSyncService: SyncService {
    
    private let syncStatusSubject = CurrentValueSubject<SyncStatus, Never>(.disabled)
    
    var syncStatusPublisher: AnyPublisher<SyncStatus, Never> {
        syncStatusSubject.eraseToAnyPublisher()
    }
    
    var isEnabled: Bool = false {
        didSet {
            syncStatusSubject.send(isEnabled ? .synced(Date()) : .disabled)
        }
    }
    
    func enableSync() async throws {
        isEnabled = true
        syncStatusSubject.send(.synced(Date()))
    }
    
    func disableSync() async throws {
        isEnabled = false
        syncStatusSubject.send(.disabled)
    }
    
    func syncNow() async throws {
        guard isEnabled else { return }
        syncStatusSubject.send(.synced(Date()))
    }
    
    func resolveConflict(with resolution: ConflictResolution) async throws {
        // Local sync doesn't have conflicts
    }
}
```

#### 3.1.4.5 iCloud 同步服务（修复：通过 Repository 操作数据）

**文件**：`Features/Sync/Services/CloudSyncService.swift`

```swift
import Foundation
import Combine
import CloudKit

class CloudSyncService: SyncService {
    
    private let transactionRepository: TransactionRepository
    private let budgetRepository: BudgetRepository
    private let accountRepository: AccountRepository
    private let container: CKContainer
    private let database: CKDatabase
    private let syncStatusSubject = CurrentValueSubject<SyncStatus, Never>(.notConfigured)
    
    var syncStatusPublisher: AnyPublisher<SyncStatus, Never> {
        syncStatusSubject.eraseToAnyPublisher()
    }
    
    var isEnabled: Bool = false {
        didSet {
            Task {
                if isEnabled {
                    try? await enableSync()
                } else {
                    try? await disableSync()
                }
            }
        }
    }
    
    init(
        transactionRepository: TransactionRepository,
        budgetRepository: BudgetRepository,
        accountRepository: AccountRepository
    ) {
        self.transactionRepository = transactionRepository
        self.budgetRepository = budgetRepository
        self.accountRepository = accountRepository
        self.container = CKContainer(identifier: "iCloud.com.yourcompany.SpendFlow")
        self.database = container.privateCloudDatabase
        
        checkiCloudStatus()
    }
    
    private func checkiCloudStatus() {
        Task {
            do {
                let status = try await container.accountStatus()
                switch status {
                case .available:
                    syncStatusSubject.send(.disabled)
                case .noAccount:
                    syncStatusSubject.send(.notConfigured)
                default:
                    syncStatusSubject.send(.failed(.notSignedIn))
                }
            } catch {
                syncStatusSubject.send(.failed(.unknown(error.localizedDescription)))
            }
        }
    }
    
    func enableSync() async throws {
        syncStatusSubject.send(.syncing)
        
        do {
            let status = try await container.accountStatus()
            guard status == .available else {
                throw SyncError.notSignedIn
            }
            
            try await performInitialSync()
            
            isEnabled = true
            syncStatusSubject.send(.synced(Date()))
        } catch let error as SyncError {
            syncStatusSubject.send(.failed(error))
            throw error
        } catch {
            let syncError = SyncError.unknown(error.localizedDescription)
            syncStatusSubject.send(.failed(syncError))
            throw syncError
        }
    }
    
    func disableSync() async throws {
        isEnabled = false
        syncStatusSubject.send(.disabled)
    }
    
    func syncNow() async throws {
        guard isEnabled else { return }
        
        syncStatusSubject.send(.syncing)
        
        do {
            try await syncTransactions()
            try await syncBudgets()
            try await syncAccounts()
            
            syncStatusSubject.send(.synced(Date()))
        } catch {
            let syncError = SyncError.unknown(error.localizedDescription)
            syncStatusSubject.send(.failed(syncError))
            throw syncError
        }
    }
    
    func resolveConflict(with resolution: ConflictResolution) async throws {
        syncStatusSubject.send(.syncing)
        
        switch resolution {
        case .keepLocal:
            try await uploadLocalData()
        case .keepRemote:
            try await downloadRemoteData()
        case .merge:
            try await mergeData()
        }
        
        syncStatusSubject.send(.synced(Date()))
    }
    
    private func performInitialSync() async throws {
        let hasRemoteData = try await checkRemoteData()
        
        if hasRemoteData {
            syncStatusSubject.send(.conflict)
            throw SyncError.conflictDetected
        } else {
            try await uploadLocalData()
        }
    }
    
    private func syncTransactions() async throws {
        // ✅ 修复：通过 Repository 获取数据
        let transactions = transactionRepository.fetchAll()
        
        for transaction in transactions {
            let record = CKRecord(
                recordType: "Transaction",
                recordID: CKRecord.ID(recordName: transaction.id.uuidString)
            )
            record["amount"] = transaction.amount
            record["category"] = transaction.category
            record["date"] = transaction.date
            record["note"] = transaction.note ?? ""
            record["type"] = transaction.type.rawValue
            
            try await database.save(record)
        }
    }
    
    private func syncBudgets() async throws {
        // Similar to syncTransactions
    }
    
    private func syncAccounts() async throws {
        // Similar to syncTransactions
    }
    
    private func checkRemoteData() async throws -> Bool {
        let query = CKQuery(recordType: "Transaction", predicate: NSPredicate(value: true))
        let (results, _) = try await database.records(matching: query)
        return !results.isEmpty
    }
    
    private func uploadLocalData() async throws {
        try await syncTransactions()
        try await syncBudgets()
        try await syncAccounts()
    }
    
    private func downloadRemoteData() async throws {
        // Download and save through Repository
    }
    
    private func mergeData() async throws {
        // Merge local and remote data
    }
}
```

#### 3.1.4.6 同步视图模型

**文件**：`Core/Presentation/ViewModels/SyncViewModel.swift`

```swift
import Foundation
import Combine

@MainActor
class SyncViewModel: ObservableObject {
    
    @Published var syncStatus: SyncStatus = .notConfigured
    @Published var isSyncEnabled: Bool = false
    @Published var isLoading: Bool = false
    @Published var errorMessage: String?
    
    private let syncService: SyncService
    private var cancellables = Set<AnyCancellable>()
    
    init(syncService: SyncService) {
        self.syncService = syncService
        
        setupBindings()
        loadSyncPreference()
    }
    
    private func setupBindings() {
        syncService.syncStatusPublisher
            .receive(on: DispatchQueue.main)
            .assign(to: &$syncStatus)
        
        $isSyncEnabled
            .sink { [weak self] isEnabled in
                self?.syncService.isEnabled = isEnabled
                self?.saveSyncPreference(isEnabled)
            }
            .store(in: &cancellables)
    }
    
    private func loadSyncPreference() {
        isSyncEnabled = UserDefaults.standard.bool(forKey: "iCloudSyncEnabled")
    }
    
    private func saveSyncPreference(_ enabled: Bool) {
        UserDefaults.standard.set(enabled, forKey: "iCloudSyncEnabled")
    }
    
    func toggleSync() {
        Task {
            isLoading = true
            errorMessage = nil
            
            do {
                if isSyncEnabled {
                    try await syncService.enableSync()
                } else {
                    try await syncService.disableSync()
                }
            } catch {
                errorMessage = error.localizedDescription
                isSyncEnabled.toggle()
            }
            
            isLoading = false
        }
    }
    
    func syncNow() {
        Task {
            isLoading = true
            errorMessage = nil
            
            do {
                try await syncService.syncNow()
            } catch {
                errorMessage = error.localizedDescription
            }
            
            isLoading = false
        }
    }
    
    func resolveConflict(with resolution: ConflictResolution) {
        Task {
            isLoading = true
            
            do {
                try await syncService.resolveConflict(with: resolution)
            } catch {
                errorMessage = error.localizedDescription
            }
            
            isLoading = false
        }
    }
}
```

#### 3.1.4.7 同步设置视图

**文件**：`Features/Sync/Views/SyncSettingsView.swift`

```swift
import SwiftUI

struct SyncSettingsView: View {
    @StateObject private var viewModel: SyncViewModel
    @Environment(\.dismiss) private var dismiss
    
    init(syncService: SyncService) {
        _viewModel = StateObject(wrappedValue: SyncViewModel(syncService: syncService))
    }
    
    var body: some View {
        NavigationStack {
            List {
                Section {
                    SyncToggle(
                        isEnabled: $viewModel.isSyncEnabled,
                        status: viewModel.syncStatus,
                        isLoading: viewModel.isLoading
                    ) {
                        viewModel.toggleSync()
                    }
                    
                    if viewModel.isSyncEnabled {
                        SyncStatusIndicator(status: viewModel.syncStatus)
                        
                        Button {
                            viewModel.syncNow()
                        } label: {
                            HStack {
                                Image(systemName: "arrow.triangle.2.circlepath")
                                Text("Sync Now")
                                Spacer()
                                if viewModel.isLoading {
                                    ProgressView()
                                }
                            }
                        }
                        .disabled(viewModel.isLoading)
                    }
                } header: {
                    Text("iCloud Sync")
                } footer: {
                    Text("Enable iCloud sync to access your data across all your devices. Your data remains on your device if sync is disabled.")
                }
                
                if let errorMessage = viewModel.errorMessage {
                    Section {
                        HStack {
                            Image(systemName: "exclamationmark.triangle.fill")
                                .foregroundStyle(.expenseRed)
                            Text(errorMessage)
                                .font(.subheadline)
                                .foregroundStyle(.secondary)
                        }
                    }
                }
                
                if viewModel.syncStatus == .conflict {
                    Section {
                        NavigationLink {
                            ConflictResolutionView(viewModel: viewModel)
                        } label: {
                            HStack {
                                Image(systemName: "exclamationmark.triangle.fill")
                                    .foregroundStyle(.warningOrange)
                                Text("Resolve Sync Conflict")
                            }
                        }
                    }
                }
            }
            .navigationTitle("Sync Settings")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .navigationBarTrailing) {
                    Button("Done") {
                        dismiss()
                    }
                }
            }
        }
    }
}

struct ConflictResolutionView: View {
    @ObservedObject var viewModel: SyncViewModel
    @Environment(\.dismiss) private var dismiss
    
    var body: some View {
        List {
            Section {
                Text("A sync conflict was detected. Please choose how to resolve it.")
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
            }
            
            Section {
                Button {
                    viewModel.resolveConflict(with: .keepLocal)
                    dismiss()
                } label: {
                    VStack(alignment: .leading, spacing: 8) {
                        Text("Keep Local Data")
                            .font(.headline)
                        Text("Replace remote data with your local data")
                            .font(.caption)
                            .foregroundStyle(.secondary)
                    }
                }
                
                Button {
                    viewModel.resolveConflict(with: .keepRemote)
                    dismiss()
                } label: {
                    VStack(alignment: .leading, spacing: 8) {
                        Text("Keep Remote Data")
                            .font(.headline)
                        Text("Replace local data with the remote data")
                            .font(.caption)
                            .foregroundStyle(.secondary)
                    }
                }
                
                Button {
                    viewModel.resolveConflict(with: .merge)
                    dismiss()
                } label: {
                    VStack(alignment: .leading, spacing: 8) {
                        Text("Merge Data")
                            .font(.headline)
                        Text("Combine local and remote data")
                            .font(.caption)
                            .foregroundStyle(.secondary)
                    }
                }
            }
        }
        .navigationTitle("Resolve Conflict")
    }
}
```

#### 3.1.4.8 同步状态指示器组件

**文件**：`Features/Sync/Views/Components/SyncStatusIndicator.swift`

```swift
import SwiftUI

struct SyncStatusIndicator: View {
    let status: SyncStatus
    
    var body: some View {
        HStack(spacing: DesignTokens.Spacing.s) {
            Image(systemName: status.icon)
                .foregroundStyle(status.color)
                .font(.title3)
            
            VStack(alignment: .leading, spacing: 2) {
                Text("Sync Status")
                    .font(.subheadline.bold())
                Text(status.displayText)
                    .font(.caption)
                    .foregroundStyle(.secondary)
            }
            
            Spacer()
        }
    }
}
```

#### 3.1.4.9 同步开关组件

**文件**：`Features/Sync/Views/Components/SyncToggle.swift`

```swift
import SwiftUI

struct SyncToggle: View {
    @Binding var isEnabled: Bool
    let status: SyncStatus
    let isLoading: Bool
    let onChange: () -> Void
    
    var body: some View {
        HStack(spacing: DesignTokens.Spacing.m) {
            Image(systemName: "icloud")
                .font(.title2)
                .foregroundStyle(isEnabled ? Color.accentBlue : .secondary)
                .frame(width: 44, height: 44)
                .background(isEnabled ? Color.accentBlue.opacity(0.15) : Color(.systemGray6))
                .clipShape(RoundedRectangle(cornerRadius: 12, style: .continuous))
            
            VStack(alignment: .leading, spacing: 2) {
                Text("iCloud Sync")
                    .font(.subheadline.bold())
                Text(isEnabled ? "Enabled" : "Disabled")
                    .font(.caption)
                    .foregroundStyle(.secondary)
            }
            
            Spacer()
            
            if isLoading {
                ProgressView()
            } else {
                Toggle("", isOn: $isEnabled)
                    .labelsHidden()
                    .onChange(of: isEnabled) { _, _ in
                        onChange()
                    }
            }
        }
    }
}
```

### 3.1.5 集成到现有代码

#### 3.1.5.1 更新 SettingsView

**文件**：`Features/Settings/Views/SettingsView.swift`

**修改内容**：
```swift
struct SettingsView: View {
    private let budgetRepository: BudgetRepository
    private let transactionRepository: TransactionRepository
    private let syncService: SyncService
    
    init(
        budgetRepository: BudgetRepository,
        transactionRepository: TransactionRepository,
        syncService: SyncService
    ) {
        self.budgetRepository = budgetRepository
        self.transactionRepository = transactionRepository
        self.syncService = syncService
    }
    
    var body: some View {
        NavigationStack {
            Form {
                Section("Sync") {
                    NavigationLink {
                        SyncSettingsView(syncService: syncService)
                    } label: {
                        SettingsRow(
                            icon: "icloud",
                            iconColor: .accentBlue,
                            title: "iCloud Sync"
                        )
                    }
                }
                
                Section("Budget") {
                    NavigationLink {
                        BudgetSettingsView(budgetRepository: budgetRepository)
                    } label: {
                        SettingsRow(icon: "slider.horizontal.3", iconColor: .accentBlue, title: "Budget Settings")
                    }
                }
                
                // ... 其他现有代码保持不变
            }
            .navigationTitle("Settings")
        }
    }
}
```

#### 3.1.5.2 更新 ContentView

**文件**：`ContentView.swift`

**修改内容**：
```swift
struct ContentView: View {
    @State private var selectedTab = 0
    
    private let transactionRepository: TransactionRepository
    private let budgetRepository: BudgetRepository
    private let accountRepository: AccountRepository
    private let syncService: SyncService
    
    init() {
        let stack = CoreDataStack.shared
        transactionRepository = TransactionRepositoryImpl(coreDataStack: stack)
        budgetRepository = BudgetRepositoryImpl(coreDataStack: stack)
        accountRepository = AccountRepositoryImpl(coreDataStack: stack)
        
        let iCloudEnabled = UserDefaults.standard.bool(forKey: "iCloudSyncEnabled")
        if iCloudEnabled {
            syncService = CloudSyncService(
                transactionRepository: transactionRepository,
                budgetRepository: budgetRepository,
                accountRepository: accountRepository
            )
        } else {
            syncService = LocalSyncService()
        }
    }
    
    var body: some View {
        TabView(selection: $selectedTab) {
            DashboardView(
                transactionRepository: transactionRepository,
                budgetRepository: budgetRepository,
                accountRepository: accountRepository
            )
            .tabItem {
                Label("Home", systemImage: "house.fill")
            }
            .tag(0)

            TransactionListView(transactionRepository: transactionRepository)
                .tabItem {
                    Label("Transactions", systemImage: "list.bullet.rectangleportait")
                }
                .tag(1)

            BudgetOverviewView(
                transactionRepository: transactionRepository,
                budgetRepository: budgetRepository
            )
            .tabItem {
                Label("Budget", systemImage: "chart.pie.fill")
            }
            .tag(2)

            AccountsListView(accountRepository: accountRepository)
                .tabItem {
                    Label("Accounts", systemImage: "wallet.pass.fill")
                }
                .tag(3)

            SettingsView(
                budgetRepository: budgetRepository,
                transactionRepository: transactionRepository,
                syncService: syncService
            )
            .tabItem {
                Label("Settings", systemImage: "gearshape.fill")
            }
            .tag(4)
        }
        .tint(.accentColor)
    }
}
```

### 3.1.6 CoreDataStack 修改（支持测试）

**文件**：`Core/Data/Persistence/CoreDataStack.swift`

**修改内容**：
```swift
import CoreData

class CoreDataStack {
    static let shared = CoreDataStack()

    let container: NSPersistentContainer

    private init(inMemory: Bool = false) {
        container = NSPersistentContainer(name: "SpendFlow")

        if inMemory {
            container.persistentStoreDescriptions.first?.url = URL(fileURLWithPath: "/dev/null")
        }

        container.loadPersistentStores { _, error in
            if let error {
                fatalError("CoreDataStack failed to load: \(error.localizedDescription)")
            }
        }

        container.viewContext.automaticallyMergesChangesFromParent = true
        container.viewContext.mergePolicy = NSMergeByPropertyObjectTrumpMergePolicy
    }
    
    // ✅ 新增：用于测试的初始化方法
    static func forTesting() -> CoreDataStack {
        CoreDataStack(inMemory: true)
    }

    var viewContext: NSManagedObjectContext {
        container.viewContext
    }

    func newBackgroundContext() -> NSManagedObjectContext {
        container.newBackgroundContext()
    }

    func save() {
        guard viewContext.hasChanges else { return }

        do {
            try viewContext.save()
        } catch {
            print("CoreData save error: \(error.localizedDescription)")
        }
    }
}
```

### 3.1.7 测试用例

#### 3.1.7.1 单元测试

**文件**：`SpendFlowTests/SyncTests/SyncServiceTests.swift`

```swift
import XCTest
@testable import SpendFlow

class SyncServiceTests: XCTestCase {
    
    var sut: LocalSyncService!
    
    override func setUp() {
        super.setUp()
        sut = LocalSyncService()
    }
    
    override func tearDown() {
        sut = nil
        super.tearDown()
    }
    
    func testInitialSyncStatusIsDisabled() {
        XCTAssertFalse(sut.isEnabled)
    }
    
    func testEnableSync() async throws {
        try await sut.enableSync()
        XCTAssertTrue(sut.isEnabled)
    }
    
    func testDisableSync() async throws {
        try await sut.enableSync()
        try await sut.disableSync()
        XCTAssertFalse(sut.isEnabled)
    }
    
    func testSyncStatusPublisher() async throws {
        let expectation = XCTestExpectation(description: "Sync status updated")
        var receivedStatus: SyncStatus?
        
        let cancellable = sut.syncStatusPublisher
            .sink { status in
                receivedStatus = status
                expectation.fulfill()
            }
        
        try await sut.enableSync()
        
        await fulfillment(of: [expectation], timeout: 1.0)
        XCTAssertNotNil(receivedStatus)
        
        cancellable.cancel()
    }
}
```

#### 3.1.7.2 集成测试

**文件**：`SpendFlowTests/SyncTests/SyncIntegrationTests.swift`

```swift
import XCTest
@testable import SpendFlow

class SyncIntegrationTests: XCTestCase {
    
    var viewModel: SyncViewModel!
    var syncService: LocalSyncService!
    
    override func setUp() {
        super.setUp()
        syncService = LocalSyncService()
        viewModel = SyncViewModel(syncService: syncService)
    }
    
    override func tearDown() {
        viewModel = nil
        syncService = nil
        super.tearDown()
    }
    
    func testToggleSync() async {
        XCTAssertFalse(viewModel.isSyncEnabled)
        
        viewModel.isSyncEnabled = true
        viewModel.toggleSync()
        
        await Task.yield()
        XCTAssertTrue(viewModel.isSyncEnabled)
    }
    
    func testSyncNow() async {
        viewModel.isSyncEnabled = true
        viewModel.syncNow()
        
        await Task.yield()
        XCTAssertFalse(viewModel.isLoading)
        XCTAssertNil(viewModel.errorMessage)
    }
}
```

### 3.1.8 验证标准

#### 3.1.8.1 功能验证

- [ ] 默认情况下，iCloud 同步处于禁用状态
- [ ] 用户可以在设置中开启/关闭 iCloud 同步
- [ ] 开启同步时，数据成功上传到 iCloud
- [ ] 关闭同步时，数据保留在本地
- [ ] 同步状态正确显示
- [ ] 同步失败时显示错误信息
- [ ] 冲突检测和解决功能正常工作
- [ ] 模拟器环境下应用正常运行（使用本地存储）

#### 3.1.8.2 性能验证

- [ ] 同步操作不阻塞 UI（在后台线程执行）
- [ ] 大量数据同步时显示进度指示器
- [ ] 同步失败时自动重试（最多3次）
- [ ] 内存使用合理（不超过 50MB）

#### 3.1.8.3 安全验证

- [ ] 数据使用 CloudKit 私有数据库
- [ ] 用户未登录 iCloud 时，显示友好提示
- [ ] 同步偏好设置保存在 UserDefaults
- [ ] 敏感数据不记录到日志

#### 3.1.8.4 兼容性验证

- [ ] iOS 15+ 设备正常运行
- [ ] iPhone 和 iPad 布局适配
- [ ] 暗黑模式下 UI 正常显示
- [ ] 模拟器环境下正常运行

---

## 3.2 CSV 导出功能（P0）

### 3.2.1 需求描述

**用户故事**：
> 作为用户，我希望能够导出我的交易数据为 CSV 文件，以便在电脑上查看或导入到其他财务软件中。

**功能要点**：
1. 支持导出所有交易数据
2. 支持按日期范围筛选导出
3. 支持按类别筛选导出
4. 导出文件包含完整的交易信息
5. 支持分享导出文件（邮件、AirDrop 等）

**非功能需求**：
- 性能：1000 条交易导出时间 < 2秒
- 格式：符合标准 CSV 格式
- 编码：UTF-8 with BOM（兼容 Excel）

### 3.2.2 架构设计

**设计模式**：Builder Pattern

```
┌─────────────────────────────────────┐
│      CSVExportService               │
│  - exportTransactions()             │
│  - exportBudgets()                  │
│  - exportAccounts()                 │
│  - shareFile()                      │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│      CSVBuilder                     │
│  - buildHeader()                    │
│  - buildRow()                       │
│  - buildCSV()                       │
└─────────────────────────────────────┘
```

### 3.2.3 文件结构（修复后）

```
Features/
└── Export/
    ├── Views/
    │   ├── ExportView.swift
    │   └── Components/
    │       └── ExportOptionsView.swift
    └── Services/
        ├── CSVExportService.swift
        └── CSVBuilder.swift

Core/
├── Domain/
│   └── Models/
│       ├── ExportFormat.swift
│       └── ExportOptions.swift
└── Presentation/
    └── ViewModels/
        └── ExportViewModel.swift
```

### 3.2.4 代码实现

#### 3.2.4.1 导出格式枚举

**文件**：`Core/Domain/Models/ExportFormat.swift`

```swift
import Foundation

enum ExportFormat: String, CaseIterable {
    case csv = "CSV"
    case json = "JSON"
    
    var fileExtension: String {
        switch self {
        case .csv: return "csv"
        case .json: return "json"
        }
    }
    
    var mimeType: String {
        switch self {
        case .csv: return "text/csv"
        case .json: return "application/json"
        }
    }
}
```

#### 3.2.4.2 导出选项

**文件**：`Core/Domain/Models/ExportOptions.swift`

```swift
import Foundation

struct ExportOptions {
    var dateRange: DateRangeOption
    var categories: [String]?
    var transactionTypes: [TransactionType]
    var includeNotes: Bool
    
    enum DateRangeOption: String, CaseIterable {
        case allTime = "All Time"
        case thisMonth = "This Month"
        case lastMonth = "Last Month"
        case thisYear = "This Year"
        case custom = "Custom Range"
    }
    
    static var `default`: ExportOptions {
        ExportOptions(
            dateRange: .allTime,
            categories: nil,
            transactionTypes: TransactionType.allCases,
            includeNotes: true
        )
    }
}
```

#### 3.2.4.3 CSV 构建器

**文件**：`Features/Export/Services/CSVBuilder.swift`

```swift
import Foundation

class CSVBuilder {
    
    private var rows: [[String]] = []
    
    func buildHeader(_ columns: [String]) -> Self {
        rows.append(columns)
        return self
    }
    
    func buildRow(_ values: [String]) -> Self {
        rows.append(values)
        return self
    }
    
    func buildCSV() -> String {
        let csvRows = rows.map { row in
            row.map { escapeCSVField($0) }.joined(separator: ",")
        }
        
        let bom = "\u{FEFF}"
        return bom + csvRows.joined(separator: "\n")
    }
    
    private func escapeCSVField(_ field: String) -> String {
        let needsQuotes = field.contains(",") || field.contains("\"") || field.contains("\n")
        
        if needsQuotes {
            let escaped = field.replacingOccurrences(of: "\"", with: "\"\"")
            return "\"\(escaped)\""
        }
        
        return field
    }
}
```

#### 3.2.4.4 CSV 导出服务

**文件**：`Features/Export/Services/CSVExportService.swift`

```swift
import Foundation

class CSVExportService {
    
    private let transactionRepository: TransactionRepository
    
    init(transactionRepository: TransactionRepository) {
        self.transactionRepository = transactionRepository
    }
    
    func exportTransactions(options: ExportOptions) throws -> URL {
        let transactions = fetchTransactions(options: options)
        
        let csv = CSVBuilder()
            .buildHeader(["Date", "Type", "Category", "Amount", "Note"])
        
        for transaction in transactions {
            _ = csv.buildRow([
                formatDate(transaction.date),
                transaction.type.rawValue.capitalized,
                transaction.category,
                String(format: "%.2f", transaction.amount),
                transaction.note ?? ""
            ])
        }
        
        let csvString = csv.buildCSV()
        
        return try saveToFile(csvString, filename: "SpendFlow_Transactions")
    }
    
    private func fetchTransactions(options: ExportOptions) -> [Transaction] {
        var transactions = transactionRepository.fetchAll()
        
        let dateRange = calculateDateRange(options.dateRange)
        if let startDate = dateRange.start, let endDate = dateRange.end {
            transactions = transactions.filter { transaction in
                transaction.date >= startDate && transaction.date <= endDate
            }
        }
        
        if let categories = options.categories, !categories.isEmpty {
            transactions = transactions.filter { categories.contains($0.category) }
        }
        
        transactions = transactions.filter { options.transactionTypes.contains($0.type) }
        
        return transactions.sorted { $0.date > $1.date }
    }
    
    private func calculateDateRange(_ option: ExportOptions.DateRangeOption) -> (start: Date?, end: Date?) {
        let calendar = Calendar.current
        let now = Date()
        
        switch option {
        case .allTime:
            return (nil, nil)
        case .thisMonth:
            let start = now.startOfMonth
            let end = now.endOfMonth
            return (start, end)
        case .lastMonth:
            let thisMonthStart = now.startOfMonth
            let start = calendar.date(byAdding: .month, value: -1, to: thisMonthStart)!
            let end = thisMonthStart
            return (start, end)
        case .thisYear:
            let start = calendar.date(from: calendar.dateComponents([.year], from: now))!
            let end = calendar.date(byAdding: .year, value: 1, to: start)!
            return (start, end)
        case .custom:
            return (nil, nil)
        }
    }
    
    private func formatDate(_ date: Date) -> String {
        date.shortFormat
    }
    
    private func saveToFile(_ content: String, filename: String) throws -> URL {
        let tempDir = FileManager.default.temporaryDirectory
        let fileURL = tempDir.appendingPathComponent("\(filename)_\(Date().timeIntervalSince1970).csv")
        
        try content.write(to: fileURL, atomically: true, encoding: .utf8)
        
        return fileURL
    }
}
```

#### 3.2.4.5 导出视图模型

**文件**：`Core/Presentation/ViewModels/ExportViewModel.swift`

```swift
import Foundation
import SwiftUI

@MainActor
class ExportViewModel: ObservableObject {
    
    @Published var exportOptions = ExportOptions.default
    @Published var isExporting = false
    @Published var exportedFileURL: URL?
    @Published var errorMessage: String?
    @Published var showShareSheet = false
    
    private let exportService: CSVExportService
    
    init(exportService: CSVExportService) {
        self.exportService = exportService
    }
    
    func exportTransactions() {
        isExporting = true
        errorMessage = nil
        
        Task {
            do {
                let url = try exportService.exportTransactions(options: exportOptions)
                exportedFileURL = url
                showShareSheet = true
                HapticFeedback.success()
            } catch {
                errorMessage = error.localizedDescription
                HapticFeedback.error()
            }
            
            isExporting = false
        }
    }
}
```

#### 3.2.4.6 导出视图

**文件**：`Features/Export/Views/ExportView.swift`

```swift
import SwiftUI

struct ExportView: View {
    @StateObject private var viewModel: ExportViewModel
    @Environment(\.dismiss) private var dismiss
    
    init(transactionRepository: TransactionRepository) {
        let exportService = CSVExportService(transactionRepository: transactionRepository)
        _viewModel = StateObject(wrappedValue: ExportViewModel(exportService: exportService))
    }
    
    var body: some View {
        NavigationStack {
            Form {
                ExportOptionsView(options: $viewModel.exportOptions)
                
                Section {
                    HapticButton("Export to CSV") {
                        viewModel.exportTransactions()
                    }
                    .disabled(viewModel.isExporting)
                }
                
                if let errorMessage = viewModel.errorMessage {
                    Section {
                        HStack {
                            Image(systemName: "exclamationmark.triangle.fill")
                                .foregroundStyle(.expenseRed)
                            Text(errorMessage)
                                .font(.subheadline)
                                .foregroundStyle(.secondary)
                        }
                    }
                }
            }
            .navigationTitle("Export Data")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .navigationBarLeading) {
                    Button("Cancel") {
                        dismiss()
                    }
                }
            }
            .sheet(isPresented: $viewModel.showShareSheet) {
                if let url = viewModel.exportedFileURL {
                    ShareSheet(activityItems: [url])
                }
            }
        }
    }
}

struct ShareSheet: UIViewControllerRepresentable {
    let activityItems: [Any]
    
    func makeUIViewController(context: Context) -> UIActivityViewController {
        UIActivityViewController(activityItems: activityItems, applicationActivities: nil)
    }
    
    func updateUIViewController(_ uiViewController: UIActivityViewController, context: Context) {}
}
```

#### 3.2.4.7 导出选项视图

**文件**：`Features/Export/Views/Components/ExportOptionsView.swift`

```swift
import SwiftUI

struct ExportOptionsView: View {
    @Binding var options: ExportOptions
    
    var body: some View {
        Section("Export Options") {
            Picker("Date Range", selection: $options.dateRange) {
                ForEach(ExportOptions.DateRangeOption.allCases, id: \.self) { option in
                    Text(option.rawValue).tag(option)
                }
            }
            
            Toggle("Include Notes", isOn: $options.includeNotes)
        }
        
        Section("Transaction Types") {
            ForEach(TransactionType.allCases, id: \.self) { type in
                Toggle(
                    type.rawValue.capitalized,
                    isOn: Binding(
                        get: { options.transactionTypes.contains(type) },
                        set: { isEnabled in
                            if isEnabled {
                                options.transactionTypes.append(type)
                            } else {
                                options.transactionTypes.removeAll { $0 == type }
                            }
                        }
                    )
                )
            }
        }
    }
}
```

### 3.2.5 集成到现有代码

#### 3.2.5.1 更新 SettingsView

**文件**：`Features/Settings/Views/SettingsView.swift`

**修改内容**：
```swift
Section("Data") {
    SettingsRow(icon: "icloud", iconColor: .accentBlue, title: "iCloud Sync", value: "Off")
    
    NavigationLink {
        ExportView(transactionRepository: transactionRepository)
    } label: {
        SettingsRow(icon: "square.and.arrow.up", iconColor: .warningOrange, title: "Export CSV")
    }
}
```

### 3.2.6 测试用例

#### 3.2.6.1 单元测试

**文件**：`SpendFlowTests/ExportTests/CSVBuilderTests.swift`

```swift
import XCTest
@testable import SpendFlow

class CSVBuilderTests: XCTestCase {
    
    var sut: CSVBuilder!
    
    override func setUp() {
        super.setUp()
        sut = CSVBuilder()
    }
    
    override func tearDown() {
        sut = nil
        super.tearDown()
    }
    
    func testBuildCSVWithHeader() {
        let header = ["Date", "Amount", "Category"]
        let csv = sut.buildHeader(header).buildCSV()
        XCTAssertTrue(csv.contains("Date,Amount,Category"))
    }
    
    func testBuildCSVWithRows() {
        let header = ["Date", "Amount"]
        let row1 = ["2026-04-16", "100.00"]
        let row2 = ["2026-04-17", "50.00"]
        
        let csv = sut
            .buildHeader(header)
            .buildRow(row1)
            .buildRow(row2)
            .buildCSV()
        
        XCTAssertTrue(csv.contains("2026-04-16,100.00"))
        XCTAssertTrue(csv.contains("2026-04-17,50.00"))
    }
    
    func testEscapeCSVFieldWithComma() {
        let field = "Coffee, Tea"
        let csv = sut.buildRow([field]).buildCSV()
        XCTAssertTrue(csv.contains("\"Coffee, Tea\""))
    }
    
    func testEscapeCSVFieldWithQuotes() {
        let field = "He said \"Hello\""
        let csv = sut.buildRow([field]).buildCSV()
        XCTAssertTrue(csv.contains("\"He said \"\"Hello\"\"\""))
    }
    
    func testUTF8BOM() {
        let csv = sut.buildCSV()
        XCTAssertTrue(csv.hasPrefix("\u{FEFF}"))
    }
}
```

#### 3.2.6.2 集成测试

**文件**：`SpendFlowTests/ExportTests/CSVExportServiceTests.swift`

```swift
import XCTest
@testable import SpendFlow

class CSVExportServiceTests: XCTestCase {
    
    var sut: CSVExportService!
    var transactionRepository: TransactionRepository!
    var coreDataStack: CoreDataStack!
    
    override func setUp() {
        super.setUp()
        coreDataStack = CoreDataStack.forTesting()
        transactionRepository = TransactionRepositoryImpl(coreDataStack: coreDataStack)
        sut = CSVExportService(transactionRepository: transactionRepository)
    }
    
    override func tearDown() {
        sut = nil
        transactionRepository = nil
        coreDataStack = nil
        super.tearDown()
    }
    
    func testExportEmptyTransactions() throws {
        let options = ExportOptions.default
        let url = try sut.exportTransactions(options: options)
        XCTAssertTrue(FileManager.default.fileExists(atPath: url.path))
    }
    
    func testExportTransactions() throws {
        let transaction = Transaction(
            amount: 100.0,
            category: "Food",
            date: Date(),
            type: .expense
        )
        try transactionRepository.save(transaction)
        
        let options = ExportOptions.default
        let url = try sut.exportTransactions(options: options)
        
        let content = try String(contentsOf: url)
        XCTAssertTrue(content.contains("Food"))
        XCTAssertTrue(content.contains("100.00"))
    }
    
    func testExportPerformance() throws {
        for i in 0..<1000 {
            let transaction = Transaction(
                amount: Double(i),
                category: "Test",
                date: Date(),
                type: .expense
            )
            try transactionRepository.save(transaction)
        }
        
        let options = ExportOptions.default
        
        measure {
            _ = try? sut.exportTransactions(options: options)
        }
    }
}
```

### 3.2.7 验证标准

#### 3.2.7.1 功能验证

- [ ] 导出功能生成有效的 CSV 文件
- [ ] CSV 文件包含正确的列标题
- [ ] CSV 文件包含所有交易数据
- [ ] 日期范围筛选正常工作
- [ ] 类别筛选正常工作
- [ ] 交易类型筛选正常工作
- [ ] 分享功能正常工作

#### 3.2.7.2 性能验证

- [ ] 1000 条交易导出时间 < 2秒
- [ ] 导出过程中 UI 不卡顿
- [ ] 内存使用合理

#### 3.2.7.3 兼容性验证

- [ ] CSV 文件可在 Excel 中正确打开
- [ ] CSV 文件可在 Numbers 中正确打开
- [ ] CSV 文件可在 Google Sheets 中正确打开
- [ ] UTF-8 字符正确显示

---

## 四、实施计划

### 4.1 开发阶段

| 阶段 | 功能模块 | 预计时间 | 负责人 | 状态 |
|------|---------|---------|--------|------|
| Phase 1 | CSV 导出 | 2天 | - | 待开始 |
| Phase 2 | iCloud 同步 | 5天 | - | 待开始 |
| Phase 3 | 周视图 | 3天 | - | 待开始 |
| Phase 4 | 智能分类建议 | 4天 | - | 待开始 |
| Phase 5 | 消费预测 | 3天 | - | 待开始 |
| Phase 6 | 发薪日提醒 | 2天 | - | 待开始 |
| Phase 7 | 测试和优化 | 3天 | - | 待开始 |

### 4.2 测试阶段

| 测试类型 | 测试内容 | 预计时间 | 状态 |
|---------|---------|---------|------|
| 单元测试 | 所有新增模块 | 2天 | 待开始 |
| 集成测试 | 模块间交互 | 1天 | 待开始 |
| UI 测试 | 用户流程 | 1天 | 待开始 |
| 性能测试 | 性能指标验证 | 0.5天 | 待开始 |
| 兼容性测试 | 设备和系统兼容性 | 0.5天 | 待开始 |

### 4.3 发布计划

| 版本 | 发布内容 | 预计日期 | 状态 |
|------|---------|---------|------|
| v1.1.0 | CSV 导出 + iCloud 同步 | 2026-05-01 | 待发布 |
| v1.2.0 | 周视图 + 智能功能 | 2026-05-15 | 待发布 |
| v1.3.0 | 发薪日提醒 + 优化 | 2026-06-01 | 待发布 |

---

## 五、风险评估

### 5.1 技术风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| iCloud 同步冲突 | 高 | 中 | 实现完善的冲突解决机制 |
| CloudKit 配额限制 | 中 | 低 | 提供本地存储降级方案 |
| 性能问题 | 中 | 低 | 异步处理 + 进度指示 |
| 数据迁移失败 | 高 | 低 | 完善测试 + 回滚机制 |

### 5.2 用户体验风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| 同步设置复杂 | 中 | 中 | 提供清晰的引导说明 |
| 导出格式不兼容 | 低 | 低 | 支持标准 CSV 格式 |
| 功能过多导致复杂 | 中 | 中 | 保持界面简洁，功能可选 |

---

## 六、后续优化建议

### 6.1 短期优化（v1.2.0）

1. **Widget 支持**：在主屏幕显示剩余预算
2. **Watch App**：Apple Watch 快速添加交易
3. **Siri Shortcuts**：语音添加交易
4. **暗黑模式优化**：进一步优化暗黑模式下的视觉效果

### 6.2 中期优化（v1.3.0）

1. **银行 API 集成**：自动同步银行交易（需评估合规成本）
2. **家庭共享**：支持家庭成员共享预算
3. **多币种支持**：支持多种货币
4. **预算模板**：提供预设预算模板

### 6.3 长期优化（v2.0.0）

1. **AI 助手**：智能财务建议
2. **投资追踪**：简单的投资组合追踪
3. **账单提醒**：定期账单提醒
4. **财务报告**：详细的月度/年度报告

---

## 七、附录

### 7.1 相关文档

- [SpendFlow 应用深度分析报告](./分析报告.md)
- [iOS Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
- [CloudKit Documentation](https://developer.apple.com/documentation/cloudkit)
- [Core Data Programming Guide](https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/CoreData/)

### 7.2 开发规范

- 遵循 Swift API 设计指南
- 使用 SwiftLint 进行代码规范检查
- 所有公开 API 必须有文档注释
- 提交信息遵循 Conventional Commits 规范

### 7.3 测试覆盖要求

- 单元测试覆盖率 ≥ 80%
- 核心业务逻辑覆盖率 = 100%
- UI 测试覆盖主要用户流程
- 性能测试覆盖关键性能指标

---

**文档结束**

**免责声明**：本改进计划仅供参考，实际开发过程中可能需要根据具体情况进行调整。所有新增功能需经过充分测试后方可发布。
